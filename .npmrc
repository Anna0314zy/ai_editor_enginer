# 关闭 pnpm v10+ 供应链策略 minimumReleaseAge 校验
# 该策略会拒绝在 cutoff 时间内（默认 ~24h）发布的依赖，CI 上会因为
# 间接依赖（@types/node、electron-to-chromium、enhanced-resolve 等）刚发布而失败
minimum-release-age=0
onlyBuiltDependencies=esbuild
